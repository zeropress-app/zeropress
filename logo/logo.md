## Logo

The ZeroPress logo represents a content artifact — not a runtime service.

The cube symbolizes:

- Structured content
- Reproducible builds
- Durable HTML artifacts
- Immutable publishing results

ZeroPress does not serve dynamic APIs.
It produces files.

The logo reflects this philosophy.